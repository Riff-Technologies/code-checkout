{
  "name": "code-checkout-examples",
  "version": "1.0.0",
  "description": "Example applications for the CodeCheckout package",
  "scripts": {
    "start:server": "ts-node ./node-server/server-usage.ts",
    "start:browser": "cd .. && npm install && npm run build && cd examples/browser-app && npm install && npx vite",
    "start:ts-program": "ts-node ./ts-program/basic-usage.ts",
    "build": "tsc"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
